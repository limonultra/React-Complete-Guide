import React from "react"
export const person = (props) => { // podemos en lugar de importar las cosas especificamente
                            // una a una con {} como en el resto de funciones de este proyecto,
                            //indicar simplemente que la función tiene argumentos como aquí con props y luego especificar 
                            //dentro del código con un .age o .whatever 
    return (
        <p>
        props.image;
        props.age;
        </p>
    )};

export const persona = (props) => { // podemos en lugar de importar las cosas especificamente
        // una a una con {} como en el resto de funciones de este proyecto,
        //indicar simplemente que la función tiene argumentos como aquí con props y luego especificar 
        //dentro del código con un .age o .whatever 
    return (
        <p>
        props.image;
        props.age;
        </p>
    )};
